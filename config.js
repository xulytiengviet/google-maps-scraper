// Browser-only Google Places configuration.
// Set ONE browser API key here, restricted to:
//   https://xulytiengviet.github.io/google-maps-scraper/*
// Enable Maps JavaScript API + Places API (New) in the same Google Cloud project.
// This key is intentionally a browser key: protect it with HTTP referrer + API restrictions.
window.POI_BROWSER_CONFIG = {
  googleMapsApiKey: ""
};
